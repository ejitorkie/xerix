xerix
=====

xerix is a Bukkit Minecraft plugin that is being developed by xeno551 and haderix. The function of the plugin is to allow server administrators the ability to code their own servers using the programming language lua.